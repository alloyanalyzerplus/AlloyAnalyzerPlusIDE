package edu.mit.csail.sdg.alloy4;

import javax.swing.JToolBar;

public class ToolbarPanel extends JToolBar {

    public ToolbarPanel() {
        super();
    }

    /*
     * @Override protected void paintComponent(Graphics g) {
     * super.paintComponent(g); Dimension size = this.getSize(); ImageIcon iCon =
     * (ImageIcon) OurUtil.loadIcon("images/bg.png"); g.drawImage(iCon.getImage(),
     * 0,0, size.width, size.height, this); }
     */
}
